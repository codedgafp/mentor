define(['jquery'], function ($) {
    return {
        init: function () {
            // Clear cookies and session storage on user logout.
            $('.usermenu a[data-title="logout,moodle"]').on('click', function() {
                console.log('logout');
            });
        }
    };
});